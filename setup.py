from setuptools import setup, find_packages

setup(
    name="json__circuit_builder",
    version="0.1.0",
    description="Hydraulic circuit JSON builder",
    packages=find_packages(),
    python_requires=">=3.10",
)
